<%= render 'refinery/admin/search_header', :url => refinery.project_individuals_admin_project_individuals_path %>
<div class='pagination_container'>
  <% if @project_individuals.any? %>
    <%= render 'project_individuals' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
